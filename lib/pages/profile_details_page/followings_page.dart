import 'package:flutter/material.dart';

class FollowingsPage extends StatelessWidget {
  const FollowingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Followings', style: TextStyle(color: Colors.black)),
    );
  }
}
